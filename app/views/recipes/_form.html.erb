<%= form_for @recipe do |f| %>
  Title: <%= f.text_field :title %>

  Ingredients:
  <%= f.fields_for :ingredients do |ingredient| %>
    Name: <%= ingredient_builder.text_field :name %>
    Quantity: <%= ingredient_builder.text_field :quantity %>
    <br>
  <% end %>

  <%= f.submit  %>
<% end %>
